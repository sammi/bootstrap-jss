import { srOnly as seOnlyMixin, srOnlyFocusable as srOnlyFocusableMixin } from '../Mixins/screenReader'

const srOnly = seOnlyMixin

const srOnlyFocusable = srOnlyFocusableMixin

export {
  srOnly,
  srOnlyFocusable
}